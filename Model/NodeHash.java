package Model;

public class NodeHash<T> {
          int chave;
          T valor;
          public NodeHash(T valor, int chave) {
               this.valor = valor;
               this.chave = chave;
          }
}
